import React, { useState } from "react";
import Head from "next/head";

const SideBar = () => {

  return (
    <div>
      <h1>SearchBar</h1>
      <h2>searchbar + button</h2>
    </div>
  );
}

export default SideBar;